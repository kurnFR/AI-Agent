Rule 1  Never modify multiple architectural layers in one step.

Rule 2  Every new component gets an isolated test.

Rule 3  Existing interfaces are inspected before integration.

Rule 4  Compile before test.

Rule 5  Test before commit.

Rule 6  Review actual source before commit.

Rule 7  Every architectural milestone gets a Git commit.

Rule 8  Never silently break an existing interface.

Rule 9  Secrets never enter source control.

Rule 10 Every execution component must have explicit failure handling.